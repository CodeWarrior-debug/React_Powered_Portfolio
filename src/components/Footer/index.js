import React from "react";

function Footer() {
  return (
    <div>
      Developed By CodeWarrior-debug
      {/* Is the CodeWarrior at war against the code, or is the CodeWarrior using the code in the war?  */}
      {/* Yes. I'll repeat: Yes. For 'yes' is the only possible answer to that silly, noob question. */}
    </div>
  );
}

export default Footer;
